/**
 * `@shadow/web` scaffold shell. The map, panels and state wiring land in
 * issue #18.
 */
export function App() {
  return (
    <main className="p-6">
      <h1 className="text-xl font-semibold">Shadow Theater</h1>
      <p>Client scaffold — map and panels land in issue #18.</p>
    </main>
  );
}
