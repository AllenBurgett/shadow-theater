/**
 * `@shadow/engine` scaffold entry point.
 *
 * The deterministic rules core (RNG, scenario loading, order validation,
 * turn resolution, projections, replay) lands in issues #13 and #14.
 */
export const ENGINE_SCAFFOLD = true as const;
